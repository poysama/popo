# default popo config; used in popo init
popo:
  git_repo: git@igit.caresharing:popo/popo.git
  rvm_repo: git@igit.caresharing:popo/rvm.git
  tools_repo: git@igit.caresharing:popo/tools.git
  src_conf: https://popo.caresharing.eu
  rvm_stable: ree-1.8.7
  dir_list: [ 'apps', 'assets', 'frameworks', 'plugins', 'helpers', 'external', 'configs', 'out/logs', 'out/tmp/pids', 'out/tmp/caches']
  gem_repo: http://gems.caresharing
